{
  "name": "react-native-xrpc",
  "version": "0.6.5",
  "repository": {
    "type": "git",
    "url": "https://github.com/webee/react-native-xrpc.git"
  },
  "description": "rpc, pub/sub between js and natives both way and some helpers for react-native.",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "babel -w js --out-dir dist",
    "build": "babel --compact true --minified js --out-dir dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "react-native",
    "bidirectional",
    "rpc",
    "pub/sub",
    "event",
    "api",
    "helper"
  ],
  "author": "webee.yw <webee.yw@gmail.com>",
  "license": "MIT",
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-react-native-stage-0": "^1.0.1"
  }
}
